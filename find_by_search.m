function search_results = find_by_search(key1, varargin)

    % this will let us check if any of the job titles contain any of the
    % keywords the user has entered
    isempty(strfind(largeString1, smallSubstring))
end